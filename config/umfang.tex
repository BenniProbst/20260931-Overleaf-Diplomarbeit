% =====================================================================
%  UMFANG-SCHALTER der Diplomarbeit (Owner-Order 346, 21.09.2026)
%  Werte:  lang  (Langfassung, vollstaendig)  |  kurz  (Kurzfassung = Abgabefassung)
%
%  Diese eine Zeile umschalten, um den Umfang zu wechseln (z. B. auf Overleaf).
%  Lokal kann der Wert per Build-Skript ueberschrieben werden:
%      pwsh -File build.ps1 -Lang de -Umfang kurz
%  Mechanik wie beim Sprach-Schalter (config/language.tex): \def vor \input gewinnt.
% =====================================================================
\providecommand{\thesisumfang}{lang}
