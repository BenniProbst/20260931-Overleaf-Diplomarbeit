% AUTO-GENERATED durch diagram_generator (HONEST-EMPTY (Groesse): Flaeche unter 2x2)
% Zelle(n) GEMESSEN, aber die Flaeche liegt unter dem 2x2-Minimum von matrix input=image
% (pgfplots bricht bei 1 Zeile ODER 1 Spalte kompilier-fatal ab). Der Vermerk behauptet
% ausdruecklich KEINE Datenlosigkeit: der Messwert steht im Korpus.
\begin{figure}[!htbp]
\centering
\emph{(Measured, but not drawable as a surface: overall latency (ns/op) spans only a 1x1 matrix (search algorithms x workloads) in the present corpus; the pgfplots surface form requires at least 2x2. The measured value itself is present in the corpus; the surface is omitted honestly instead of failing the build.)}
\caption{Heatmap: overall latency (ns/op) (search algorithm x workload)}
\end{figure}
