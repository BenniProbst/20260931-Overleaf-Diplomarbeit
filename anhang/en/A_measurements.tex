\chapter{Complete Measurement Series}\label{app:measurements}

% AUTO-FILLED: the tables + diagrams below are produced by the orchestrator
% generate_measurement_appendix.ps1 from a measurements.csv (16-column schema,
% Path A / run\_workload) and written to anhang/en/tabellen/. The language follows
% \thesislang (en): column headers + axis labels are localised.
% To add a measurement series: run the orchestrator with -SpecId <id> and add the
% two \input lines (table + diagram) here.

This appendix contains the complete measurement series of the pre-configured organism permutations.
Table~\ref{tab:measurements:v5_pipeline_demo} lists the metrics tabulated per permutation
(operations; clock cycles approximated; cache misses L1--L3 currently 0/not collected, cf.\
limitation~1 in Section~\ref{sec:measurements:limitations}); Figure~\ref{fig:measurements:v5_pipeline_demo}
compares the clock cycles graphically.

\input{anhang/en/tabellen/v5_pipeline_demo_table.tex}
\input{anhang/en/tabellen/v5_pipeline_demo_diagram.tex}

\section{Cartesian smoke series (43 permutations, real DLL measurement)}

The following series results from the full end-to-end chain: the built permutation DLLs (43
combinations in two sub-blocks: 27 cartesian SIMD$\times$layout$\times$allocator combinations plus 16
more over node width, path compression, internal search, and telemetry ($2^4$) --- in total seven
varied of the nineteen axes) were measured at the test dock,
persisted as binary measurement records, converted via pipeline stage~03 into the 16-column schema,
and embedded here automatically. Table~\ref{tab:measurements:cartesian_smoke43} and
Figure~\ref{fig:measurements:cartesian_smoke43} show the results.

\input{anhang/en/tabellen/cartesian_smoke43_table.tex}
\input{anhang/en/tabellen/cartesian_smoke43_diagram.tex}

% =====================================================================
%  L-f: evaluated measurement artefacts (bias break / latency surfaces /
%  axis exchangeability / limitations). The tables + diagrams below are
%  produced from the WIDE schema (full 19-axis tuples + op_<kind>_p50
%  columns + two_phase_valid), not from the 16-column schema;
%  le_limitierung is static (no CSV). Everything lives symmetrically
%  under anhang/en/tabellen/ resp. anhang/de/tabellen/.
% =====================================================================

\section{Bias-break matrix}\label{sec:measurements:bias}

Table~\ref{tab:bias:search-algo-workload} summarises the median latency (ns/op, nearest-rank)
measured per search method and load profile across all dynamic settings and repetitions; only the
runs marked \texttt{two\_phase\_valid} are taken into account. The matrix reveals under which load
profiles a given method breaks its systematic bias.

\input{anhang/en/tabellen/bias_matrix_table.tex}

\section{Latency surfaces per interface function}\label{sec:measurements:surfaces}

The following six heatmaps show the latency surface (search algorithm $\times$ load profile) per
interface function: the overall latency (\texttt{ns\_per\_op}) and the p50 latencies of the
individual operations \texttt{insert}, \texttt{lookup}, \texttt{erase}, \texttt{scan} and
\texttt{rmw}.

\input{anhang/en/tabellen/lc_surface_ns_per_op.tex}
\input{anhang/en/tabellen/lc_surface_op_insert_p50_ns.tex}
\input{anhang/en/tabellen/lc_surface_op_lookup_p50_ns.tex}
\input{anhang/en/tabellen/lc_surface_op_erase_p50_ns.tex}
\input{anhang/en/tabellen/lc_surface_op_scan_p50_ns.tex}
\input{anhang/en/tabellen/lc_surface_op_rmw_p50_ns.tex}

\section{Axis exchangeability}\label{sec:measurements:exchange}

The four following longtables provide the exchangeability evidence per variable axis from the
sibling-pair differences (median rel.\ $\Delta$ ns/op w.r.t.\ $v$): \texttt{search\_algo}
(Table~\ref{tab:ld:exchange:search_algo}), \texttt{node\_type}
(Table~\ref{tab:ld:exchange:node_type}), \texttt{memory\_layout}
(Table~\ref{tab:ld:exchange:memory_layout}) and \texttt{prefetch}
(Table~\ref{tab:ld:exchange:prefetch}). The least confounded are \texttt{search\_algo} and
\texttt{prefetch}.

\input{anhang/en/tabellen/ld_exchange_search_algo.tex}
\input{anhang/en/tabellen/ld_exchange_node_type.tex}
\input{anhang/en/tabellen/ld_exchange_memory_layout.tex}
\input{anhang/en/tabellen/ld_exchange_prefetch.tex}

\section{Limitations}\label{sec:measurements:limitations}

Table~\ref{tab:le:limitierung} lists all unresolved caveats of the measurement series, so that no
finding silently lapses; row~1 concerns the missing PMC core metric (cache misses).

\input{anhang/en/tabellen/le_limitierung.tex}
