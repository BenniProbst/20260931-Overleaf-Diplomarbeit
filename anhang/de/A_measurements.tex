\chapter{Vollst\"{a}ndige Messreihen}\label{app:measurements}

% AUTO-BEFUELLT: Tabellen + Diagramme werden vom Orchestrator
% generate_measurement_appendix.ps1 aus einer measurements.csv (16-Spalten-Schema,
% Pfad A / run\_workload) erzeugt und nach anhang/de/tabellen/ abgelegt. Sprache
% folgt \thesislang (de): Spaltenkoepfe + Achsenbeschriftung sind lokalisiert.
% Weitere Messreihe ergaenzen: Orchestrator mit -SpecId <id> laufen lassen und hier
% zwei \input-Zeilen (Tabelle + Diagramm) hinzufuegen.

Dieser Anhang enth\"{a}lt die vollst\"{a}ndigen Messreihen der vorkonfigurierten
Lebewesen-Permutationen. Tabelle~\ref{tab:measurements:v5_pipeline_demo} listet die je Permutation
tabellierten Kennzahlen (Operationen; Taktzyklen approximiert; Cache-Misses L1--L3 derzeit 0/nicht
erhoben, vgl.\ Limitierung~1 in Abschnitt~\ref{sec:measurements:limitations});
Abbildung~\ref{fig:measurements:v5_pipeline_demo} stellt die Taktzyklen grafisch gegen\"{u}ber.

\input{anhang/de/tabellen/v5_pipeline_demo_table.tex}
\input{anhang/de/tabellen/v5_pipeline_demo_diagram.tex}

\section{Kartesische Smoke-Messreihe (43 Permutationen, echte DLL-Messung)}

Die folgende Messreihe entstand aus dem vollst\"{a}ndigen Ketten-Durchlauf: die gebauten
Permutations-DLLs (43 Kombinationen in zwei Teilbl\"{o}cken: 27 kartesische
SIMD$\times$Layout$\times$Allokator-Kombinationen plus 16 weitere \"{u}ber Node-Breite,
Pfadkompression, interne Suche und Telemetrie ($2^4$) --- in Summe sieben variierte der neunzehn
Achsen) wurden am
Pr\"{u}f-Dock gemessen, als bin\"{a}re Mess-Records persistiert, via Pipeline-Stufe~03 in das
16-Spalten-Schema konvertiert und automatisch hier eingebunden.
Tabelle~\ref{tab:measurements:cartesian_smoke43} und
Abbildung~\ref{fig:measurements:cartesian_smoke43} zeigen die Ergebnisse.

\input{anhang/de/tabellen/cartesian_smoke43_table.tex}
\input{anhang/de/tabellen/cartesian_smoke43_diagram.tex}

% =====================================================================
%  L-f: ausgewertete Mess-Artefakte (Bias-Bruch / Latenz-Surfaces /
%  Achsen-Austauschbarkeit / Limitierungen). Die Tabellen + Diagramme
%  unten werden aus dem WIDE-Schema (volle 19-Achsen-Tupel + op_<art>_p50-
%  Spalten + two_phase_valid) erzeugt, nicht aus dem 16-Spalten-Schema;
%  le_limitierung ist statisch (ohne CSV). Alles liegt symmetrisch unter
%  anhang/de/tabellen/ bzw. anhang/en/tabellen/.
% =====================================================================

\section{Bias-Bruch-Matrix}\label{sec:measurements:bias}

Tabelle~\ref{tab:bias:search-algo-workload} fasst die je Suchverfahren und Lastprofil gemessene
Median-Latenz (ns/op, nearest-rank) \"uber alle dynamischen Einstellungen und Wiederholungen
zusammen; ber\"ucksichtigt sind ausschlie\ss{}lich die als \texttt{two\_phase\_valid} markierten
L\"aufe. Die Matrix macht sichtbar, bei welchen Lastprofilen das jeweilige Verfahren seinen
systematischen Bias bricht.

\input{anhang/de/tabellen/bias_matrix_table.tex}

\section{Latenz-Surfaces je Schnittstellen-Funktion}\label{sec:measurements:surfaces}

Die folgenden sechs Heatmaps stellen die Latenz-Oberfl\"ache (Suchalgorithmus $\times$ Lastprofil)
je Schnittstellen-Funktion dar: die Gesamt-Latenz (\texttt{ns\_per\_op}) sowie die p50-Latenzen der
Einzeloperationen \texttt{insert}, \texttt{lookup}, \texttt{erase}, \texttt{scan} und \texttt{rmw}.

\input{anhang/de/tabellen/lc_surface_ns_per_op.tex}
\input{anhang/de/tabellen/lc_surface_op_insert_p50_ns.tex}
\input{anhang/de/tabellen/lc_surface_op_lookup_p50_ns.tex}
\input{anhang/de/tabellen/lc_surface_op_erase_p50_ns.tex}
\input{anhang/de/tabellen/lc_surface_op_scan_p50_ns.tex}
\input{anhang/de/tabellen/lc_surface_op_rmw_p50_ns.tex}

% =====================================================================
%  CI-interaktiv (2026-07, #24): die vier folgenden Darstellungen werden
%  vom C++23-Generator (Code/08_appendix_generator, ersetzt die .ps1-
%  Orchestratoren) je Messlauf aus der WIDE-Matrix regeneriert und von
%  der Mess-Pipeline nach measurement/<datiert>/ persistiert.
%  \InputIfFileExists: kompiliert auch VOR dem ersten Messlauf sauber.
% =====================================================================

\section{Per-Achsen-Latenz-Attribution}\label{sec:measurements:seg-attribution}

Die folgende Abbildung zerlegt die Segment-Laufzeit je Suchverfahren in die Beitr\"age der einzelnen
Organ-Achsen (gestapelte Balken). Das 100\,\%-Ganze je Balken ist die Wall-Clock des separaten
19-Segment-Laufs (\texttt{seg\_run\_total\_ns}), \emph{nicht} die Real-Workload-Latenz
(\texttt{total\_ns}); die 20 Segmente (19 Organ-Achsen + Framework-Rest) summieren sich exakt zu
dieser Basis (Coverage $\approx 1{,}0$).

\InputIfFileExists{anhang/de/tabellen/seg_attribution.tex}{}{%
  \emph{(Diese Darstellung erscheint nach dem n\"achsten Messlauf mit \texttt{seg\_*\_ns}-Spalten.)}}

\section{Latenz-Verteilung}\label{sec:measurements:latency-distribution}

Die Range-Balken zeigen je Suchverfahren und Operation die p50--p99-Spanne (Punkt bei p50, Whisker
zu p99); da nur diese beiden Perzentile gemessen werden, wird bewusst \emph{kein} Box-Plot mit
interpolierten Quartilen gezeichnet. Die ECDF stellt die Verteilung der Gesamt-Latenz \emph{\"uber
die Konfigurationen} dar (Anteil der Permutationen mit \texttt{ns\_per\_op} $\le x$) --- keine
Per-Operation-CDF, da die Rohsamples je Operation nicht vorliegen.

\InputIfFileExists{anhang/de/tabellen/latency_range.tex}{}{%
  \emph{(Die p50--p99-Range-Darstellung erscheint nach dem n\"achsten Messlauf.)}}
\InputIfFileExists{anhang/de/tabellen/latency_ecdf.tex}{}{%
  \emph{(Die Konfigurations-ECDF erscheint nach dem n\"achsten Messlauf.)}}

\section{Achsen-Austauschbarkeit}\label{sec:measurements:exchange}

Die vier folgenden Langtabellen belegen die Austauschbarkeit je variabler Achse anhand der
Geschwister-Paar-Differenzen (Median rel.\ $\Delta$ ns/op bez\"uglich $v$): \texttt{search\_algo}
(Tabelle~\ref{tab:ld:exchange:search_algo}), \texttt{node\_type}
(Tabelle~\ref{tab:ld:exchange:node_type}), \texttt{memory\_layout}
(Tabelle~\ref{tab:ld:exchange:memory_layout}) und \texttt{prefetch}
(Tabelle~\ref{tab:ld:exchange:prefetch}). Am wenigsten konfundiert sind \texttt{search\_algo} und
\texttt{prefetch}.

\input{anhang/de/tabellen/ld_exchange_search_algo.tex}
\input{anhang/de/tabellen/ld_exchange_node_type.tex}
\input{anhang/de/tabellen/ld_exchange_memory_layout.tex}
\input{anhang/de/tabellen/ld_exchange_prefetch.tex}

Der folgende Forest-Plot fasst die Austauschbarkeit der \texttt{ns\_per\_op}-Kennzahl zusammen
(Punkt = Median rel.\ $\Delta$, Whisker = IQR, 0-Referenzlinie; kleine-$n$-Zeilen ausgegraut). Der
Whisker liegt symmetrisch um den Median (\"uber die IQR-Breite), da das Aggregat nur die IQR-Breite,
nicht die Quartilsgrenzen tr\"agt.

\InputIfFileExists{anhang/de/tabellen/exchange_forest.tex}{}{%
  \emph{(Der Forest-Plot erscheint nach dem n\"achsten Messlauf.)}}

\section{Limitierungen}\label{sec:measurements:limitations}

Tabelle~\ref{tab:le:limitierung} f\"uhrt alle nicht-gefixten Vorbehalte der Messreihen auf, damit
kein Befund still verf\"allt; Zeile~1 betrifft die fehlende PMC-Kernmetrik (Cache-Misses).

\input{anhang/de/tabellen/le_limitierung.tex}
