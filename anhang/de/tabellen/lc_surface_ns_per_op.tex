% AUTO-GENERATED durch diagram_generator (HONEST-EMPTY (Groesse): Flaeche unter 2x2)
% Zelle(n) GEMESSEN, aber die Flaeche liegt unter dem 2x2-Minimum von matrix input=image
% (pgfplots bricht bei 1 Zeile ODER 1 Spalte kompilier-fatal ab). Der Vermerk behauptet
% ausdruecklich KEINE Datenlosigkeit: der Messwert steht im Korpus.
\begin{figure}[!htbp]
\centering
\emph{(Gemessen, aber nicht als Flaeche darstellbar: Gesamt-Latenz (ns/op) liegt im vorliegenden Korpus nur als $1{\times}1$-Matrix vor (Suchalgorithmen $\times$ Workloads); die pgfplots-Flaechenform verlangt mindestens $2{\times}2$. Der Messwert selbst ist im Korpus vorhanden; die Flaeche wird ehrlich ausgelassen, statt den Bau zu brechen.)}
\caption{Heatmap: Gesamt-Latenz (ns/op) (Suchalgorithmus x Workload)}
\end{figure}
