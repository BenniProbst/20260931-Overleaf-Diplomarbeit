% AUTO-GENERATED durch diagram_generator (E-2a HONEST-EMPTY: Metrik ohne Messwerte)
% KEINE Zelle dieser (search_algo x workload)-Flaeche traegt einen ausgefuehrten Messwert.
% Eine Heatmap daraus waere eine erfundene 0-ns-Flaeche (und bricht pgfplots fatal, weil die
% Farbskala auf eine entartete [0.0:0.0]-Domaene faellt). Daher: ehrlicher Vermerk statt Figur.
\begin{figure}[!htbp]
\centering
\emph{(Keine vergleichbaren Messwerte: Lookup-Latenz p50 (ns/op) liegt im vorliegenden Korpus fuer linear\_scan nicht als Referenz vor (oder wurde nie ausgefuehrt). Diese Flaeche wird ehrlich ausgelassen, statt ein Verhaeltnis gegen eine fehlende Referenz zu behaupten.)}
\caption{Verhaeltnis zur Referenz linear\_scan: Lookup-Latenz p50 (ns/op) (1 = wie die Referenz)}
\end{figure}
