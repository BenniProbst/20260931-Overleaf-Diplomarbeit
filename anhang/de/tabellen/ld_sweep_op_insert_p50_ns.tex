% AUTO-GENERATED durch diagram_generator (HONEST-EMPTY: Sweep-Metrik nie ausgefuehrt)
% Die Arbeitsmengen-Spalte working_set_n IST vorhanden, aber die Operation dieses
% z-Feldes wurde in KEINER gueltigen Konfiguration ausgefuehrt (op_<art>_n == 0). Eine
% Kurve daraus waere eine erfundene 0-ns-Messung -- daher ehrlicher Vermerk statt Figur.
\begin{figure}[!htbp]
\centering
\emph{(Keine Messwerte: die Operation dieser Metrik wurde im vorliegenden Korpus nie ausgefuehrt. Die Kurve wird ehrlich ausgelassen, statt eine 0-ns-Messung zu zeigen.)}
\caption{Working-Set-Sweep: Insert-Latenz p50 (ns/op)}
\end{figure}
