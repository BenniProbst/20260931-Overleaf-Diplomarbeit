% =====================================================================
%  Diploma thesis  —  Benjamin-Elias Probst
%  TU Dresden, Faculty of Computer Science, Institute of Systems Architecture,
%  Chair of Databases (advisors: Habich / Krause)
%  Template: ZIH LaTeX template (zihpub.cls), document type: diplominf
%
%  BILINGUAL SINGLE SOURCE — primary EN, secondary DE.
%  Language switch: \thesislang = 'en' | 'de'  (config/language.tex).
%  Build:   pwsh -File build.ps1 [-Lang en|de]   (3-pass pdflatex + bibtex)
%  Concept: sessions/2026-06-01-bilingualer-compile-schalter.md
% =====================================================================

% --- LANGUAGE SWITCH ---------------------------------------------------
% Default comes from config/language.tex (\providecommand -> 'en').
% A command-line \def (build.ps1 -Lang de) wins, because it is set BEFORE
% this \input and \providecommand does not overwrite an existing macro.
\input{config/language.tex}
\providecommand{\thesislang}{en}      % hard fallback if config missing
\edef\thesislang{\thesislang}         % normalise: strip \long so \ifx matches reliably
                                      % (config path via \providecommand vs. CLI \def)
\def\langDE{de}
\newif\ifthesisDE
\ifx\thesislang\langDE \thesisDEtrue \else \thesisDEfalse \fi
% Pass 'german' to zihpub only in DE mode (English is the template default):
\ifthesisDE \PassOptionsToClass{german}{zihpub}\fi

\documentclass[diplominf,utf8,hyperref,lof,lot,twoside]{zihpub}

% --- Additional packages (NOT loaded by zihpub.cls) -------------------
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepackage{siunitx}
\usepackage{listings}
\usepackage{microtype}
\usepackage{csquotes}                 % \enquote{...} — language-aware quotation marks

% --- Language-specific typographic conventions ------------------------
\ifthesisDE
  \sisetup{output-decimal-marker={,}, group-separator={\,}}  % 3,14  /  1\,000
  \setlist[enumerate,2]{label=\alph*)}                        % DE:  a) b) c)
\else
  \sisetup{output-decimal-marker={.}, group-separator={,}}    % 3.14  /  1,000
  \setlist[enumerate,2]{label=(\alph*)}                       % EN: (a) (b) (c)
\fi

% --- Metadata (title page + declaration generated by the class) -------
\author{Benjamin-Elias Probst}
\ifthesisDE
  \title{Aktive cache-bewusste Hardware-Adaption:\texorpdfstring{\\}{ }%
         Eine Cache-Engine für Trie-basierte Indexstrukturen}
\else
  \title{Active Cache-Aware Hardware Adaptation:\texorpdfstring{\\}{ }%
         Cache Engine for Trie-Based Index Structures}
\fi
\matno{4510512}
% Geburtsdatum in beiden Formaten: Titelseite folgt \thesislang, Erklaerung folgt \declarationlang.
\newcommand{\birthdateDE}{11.~April 1996}
\newcommand{\birthdateEN}{11~April 1996}
\ifthesisDE\birthday{\birthdateDE}\else\birthday{\birthdateEN}\fi
\placeofbirth{Potsdam}
% Hochschullehrer = apl. Prof. Habich (supervising professor + first advisor);
% Dr. Krause = second advisor.
\hsl{apl.\ Prof.\ Dr.-Ing.\ Dirk Habich}
\betreuer{apl.\ Prof.\ Dr.-Ing.\ Dirk Habich, Dr.-Ing.\ Alexander Krause}
\bibfiles{literatur}                  % style: alpha (EN) / alphadin (DE) — auto via class

% --- Thesis assignment (consolidated from the project meetings docs/termine) --
% Bilingual via the language switch; replace with the official task sheet if required.
\ifthesisDE
  \aufgabenstellung{\input{aufgabenstellung/de}}
\else
  \aufgabenstellung{\input{aufgabenstellung/en}}
\fi

% --- Declaration of authorship: SEPARATE language switch --------------------
% Values:  de | en | same_as_document  (default: same_as_document = follow \thesislang).
\providecommand{\declarationlang}{same_as_document}

% --- Abstract (only the document language is typeset; EN = source, DE = faithful). ---
\abstracten{%
Search algorithms and index structures are central to modern in-memory databases. Since the
Adaptive Radix Tree, numerous cache-aware optimisations---spanning node layout,
prefetching, succinct representations, and allocation---have been proposed, yet they
have never been combined and compared within a single, fair measurement framework:
each publication uses its own data structures, workloads, and methodology, which makes
results hard to compare across publications. This thesis presents a three-layer
cache-engine architecture (CacheEngine~$\to$~ExecutionEngine~$\to$~SearchEngine) that
decomposes cache-aware search algorithms into interchangeable design dimensions of a
large design space~\cite{idreos2018periodic, idreos2018datacalculator}. Structurally
different methods---from tree-like indexes (tries, B\textsuperscript{+}-trees) to
non-tree-like hash tables---are expressed as points of the same design space behind a
single interface; non-tree-like methods occupy a restricted subset of the dimensions.
State-of-the-art algorithms are reconstructed as explicit configurations and measured at
three granularities: individual design decisions, individual interface operations, and
whole algorithms under YCSB load profiles. As a probe, this thesis contributes PRT-ART, a
Probst Redirect Tree~/ ART hybrid with eight building-block layers of its own; a
measurement driver with profile-aware workload routing evaluates it against eight Tier-1
and several Tier-2/3 designs under three mandatory measurement series, and delivers the
best recombination as a standalone binary.}
\abstractde{%
Suchalgorithmen und Indexstrukturen sind ein Kernbaustein moderner In-Memory-Datenbanken.
Seit dem Adaptive Radix Tree wurde eine Vielzahl cache-bewusster Optimierungen
vorgeschlagen---von Knoten-Layout über Prefetching und Succinct-Repräsentationen bis
zur Allokation---, doch wurden sie nie in einem einzigen, fairen Messrahmen kombiniert
und verglichen: Jede Veröffentlichung nutzt eigene Datenstrukturen, Lastprofile und
Messmethodik, wodurch Ergebnisse über Veröffentlichungen hinweg schwer vergleichbar
sind. Diese Arbeit stellt eine dreischichtige Cache-Engine-Architektur
(CacheEngine~$\to$~ExecutionEngine~$\to$~SearchEngine) vor, die cache-bewusste
Suchalgorithmen in austauschbare Entwurfsdimensionen eines großen Entwurfsraums
(\emph{design space}) zerlegt~\cite{idreos2018periodic, idreos2018datacalculator}.
Strukturell verschiedene Verfahren---von baumartigen Indizes (Tries,
B\textsuperscript{+}-Bäume) bis zu nicht-baumartigen Hash-Tabellen---werden als Punkte
desselben Entwurfsraums hinter einem einheitlichen Interface ausgedrückt; nicht-baumartige
Verfahren belegen dabei ein eingeschränktes Subset der Dimensionen. Algorithmen des Stands
der Technik werden als explizite Konfigurationen rekonstruiert und auf drei Granularitäten
gemessen: einzelne Entwurfsentscheidungen, einzelne Interface-Operationen und ganze
Algorithmen unter YCSB-Lastprofilen. Als Prüfling trägt diese Arbeit PRT-ART bei, einen
Probst-Redirect-Tree-/ART-Hybriden mit acht eigenen Bausteinschichten; ein Mess-Treiber mit
profilbewusstem Workload-Routing bewertet ihn gegen acht Tier-1- und mehrere
Tier-2/3-Entwürfe unter drei verpflichtenden Messreihen und liefert die beste Rekombination
als eigenständige Binary aus.}
% --- Acknowledgments: single field, switched by language. --------------------
\ifthesisDE
  \acknowledgments{% TODO: Danksagung ausformulieren.
  Platzhalter -- Danksagung folgt.}
\else
  \acknowledgments{% TODO: write the acknowledgments.
  Placeholder -- acknowledgments to follow.}
\fi

% TODO(Sperrvermerk): confidentiality notice requested -- provide wording + placement.
% TODO(Aufgabenstellung): bind official task sheet (PDF) instead of the placeholder.

% --- Institution on the title page (document-side override) -----------
\makeatletter
\ifthesisDE
\renewcommand{\@institution}{%
  \begin{center}\huge\textsc{Technische Universit\"{a}t Dresden}

  \bigskip\Large
  \textsc{Fakult\"{a}t Informatik\\ Institut f\"{u}r Systemarchitektur\\
  Professur f\"{u}r Datenbanken\\ Prof.\ Dr.-Ing.\ Wolfgang Lehner\\ Prof.\ apl.\ Dr.-Ing.\ Dirk Habich}\end{center}}
\else
\renewcommand{\@institution}{%
  \begin{center}\huge\textsc{Technische Universit\"{a}t Dresden}

  \bigskip\Large
  \textsc{Faculty of Computer Science\\ Institute of Systems Architecture\\
  Chair of Databases\\ Prof.\ Dr.-Ing.\ Wolfgang Lehner\\ Prof.\ apl.\ Dr.-Ing.\ Dirk Habich}\end{center}}
\fi
\makeatother

\begin{document}

% =====================  Main matter (language-selected)  ==============
\include{kapitel/\thesislang/01_introduction}
\include{kapitel/\thesislang/02_fundamentals}
\include{kapitel/\thesislang/03_state_of_the_art}
\include{kapitel/\thesislang/04_concept_architecture}
\include{kapitel/\thesislang/05_implementation}
\include{kapitel/\thesislang/06_evaluation_methodology}
\include{kapitel/\thesislang/07_results_evaluation}
\include{kapitel/\thesislang/08_conclusion}

% \appendix auto-inserts bibliography + list of figures + list of tables before it
\appendix
\include{anhang/\thesislang/A_measurements}
\include{anhang/\thesislang/B_code_structure}
\include{anhang/\thesislang/C_glossary}
\include{anhang/\thesislang/D_building_block_matrix}
\include{anhang/\thesislang/E_architecture_decisions}

\end{document}
